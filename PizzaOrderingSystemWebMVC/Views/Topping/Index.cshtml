@model IEnumerable<PizzaOrderingSystemWebMVC.Models.Topping>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ToppingName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ToppingPrice)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ToppingName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ToppingPrice)
            </td>
            <td>
                @*<a asp-action="Edit" asp-route-id="@item.ToppingNumber">Edit</a> |*@
                <a asp-action="Details" asp-route-id="@item.ToppingNumber">Add</a> 
                @*<a asp-action="Delete" asp-route-id="@item.ToppingNumber">Delete</a>*@
            </td>
        </tr>
}
    </tbody>
</table>
